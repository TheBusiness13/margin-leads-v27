MarginBusiness Leads v5.0.2 — Smart Column Mapping

Import improvements:
- Detects the likely header row even when spreadsheets contain title rows above the table.
- Automatically maps common column names to MarginBusiness Leads fields.
- Shows a clear column-matching screen before import.
- Lets users correct every field manually.
- Supports separate first-name and last-name columns.
- Stops relying on the original fixed spreadsheet column order.
- Prevents company, contact, website and brand values from being silently shifted into the wrong fields.
- Refreshes the preview immediately after mapping changes.

No database migration is required.
