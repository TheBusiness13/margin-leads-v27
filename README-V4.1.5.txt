MarginBusiness Leads v4.1.5

Fixes:
- Email provider settings are now a visible page, not a collapsed admin panel.
- Both normal users and admins can find provider selection and API key entry under Integrations & Settings.
- Adds a direct Connect email provider shortcut.
- Improves Admin Console error handling when the deployment serves the wrong file.

Keep the v4.1.2 workspace_mail_providers migration and PROVIDER_ENCRYPTION_KEY already configured.
