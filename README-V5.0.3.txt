MarginBusiness Leads v5.0.3

Critical fixes:
- Overview Import Leads now opens the Leads page and the importer correctly.
- Adds automatic lead snapshots before imports, sequence changes and sends.
- Prevents an accidental empty campaign save from deleting an existing lead list.
- Adds Restore latest lead snapshot.
- Adds the missing live-send confirmation control.
- Rebuilds sending into four clear steps: connect provider, prepare batch, confirm, send.
- Adds a provider-readiness indicator directly inside Campaigns.
- Clarifies that switching sequences filters due leads but does not remove database leads.
- Preserves authentication, workspace isolation, smart column mapping, providers and Admin Console.

No SQL migration is required.
