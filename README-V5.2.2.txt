MarginBusiness Leads v5.2.2

Fixes:
- Shows the exact provider error for each failed recipient.
- Adds plain-language explanations for sender verification, API key, permissions, invalid recipients and rate limits.
- Keeps the raw provider response visible for diagnosis.
- Adds a reminder that the From email must be verified inside the provider.

No new SQL migration is required beyond v5.2.
