MarginBusiness Leads v4.1.6

Fixes:
- Moves Integrations & Settings outside the hidden Campaigns container.
- Provider selection and API-key fields are now visible to admins and normal users.
- Adds a direct Connect email provider shortcut.
- Removes the missing workspaces.daily_send_limit field from the Admin Console query.
- Keeps private per-workspace provider credentials and Google login.

No new database migration is required if the v4.1.2 provider migration was already run.
